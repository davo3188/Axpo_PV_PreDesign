{
  "name": "pv-predesign-headless-tests",
  "private": true,
  "type": "module",
  "description": "Runs the browser test pages headless with the CDN libraries served from local npm bundles (for machines without access to js.arcgis.com / cdn.jsdelivr.net).",
  "scripts": {
    "build": "node build.mjs",
    "test": "node run.mjs"
  },
  "devDependencies": {
    "@arcgis/core": "4.34.2",
    "@tmcw/togeojson": "5.8.1",
    "buffer": "^6.0.3",
    "esbuild": "^0.25.0",
    "geotiff": "2.1.3",
    "jszip": "3.10.1",
    "playwright": "1.56.1",
    "shpjs": "4.0.4",
    "xlsx": "0.18.5"
  }
}
